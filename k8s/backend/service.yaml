apiVersion: v1
kind: Service
metadata:
  name: emk-backend
  namespace: backend
  labels:
    app: emk-backend
spec:
  selector:
    app: emk-backend
  ports:
    - name: http
      port: 80
      targetPort: 8080